---
title: "Model overview"
format: html
---

STBLR fits traits independently; MTBLR jointly models a small trait set.

| Data level | BayesC | BayesR | Annotation-informed BayesR |
|---|---|---|---|
| Individual packed BED | `bayesc` | `bayesr` | `bayesrc` |
| Summary statistics | `sbayesc` | `sbayesr` | `sbayesrc` |

The `s` prefix means summary statistics, not MAF scaling. Optional
`selection_s` controls MAF-dependent effect-variance scaling independently.

Public fitters are `stblr_csr()`, `stblr_csr_annot()`,
`stblr_block_eigen()`, `stblr_bed()`, `mtblr_csr()`,
`mtblr_block_eigen()`, and `mtblr_bed()`.
