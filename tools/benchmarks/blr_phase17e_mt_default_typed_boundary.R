# Phase 17E reuses the canonical Phase 17D matched workloads and reporting.
source("tools/benchmarks/blr_phase17d_mt_default_extraction.R")

cat("Phase 17E ownership/copy audit:\n")
cat("  R/native input: existing wrapper conversions and by-value parameters unchanged\n")
cat("  dense XX: borrowed by const reference through MtDefaultDataView; no typed-boundary copy\n")
cat("  wy/ww/yy/n: existing by-value native inputs borrowed during the core call\n")
cat("  models/priors: existing by-value native inputs borrowed during the core call\n")
cat("  initial b/B/E/pi: moved from by-value native parameters into core-owned state\n")
cat("  core result: owns final state, workspaces required by finalization, traces, and counts\n")
cat("  legacy finalizer: reads the typed result and borrowed wy; schema allocation unchanged\n")
cat("  no new O(nt x m^2) copy; no MCMC-time file I/O\n")
cat("completed-fit RSS is not peak RSS\n")
cat("dense XX remains O(nt x m^2)\n")
cat("tiny timings are regression signals\n")
