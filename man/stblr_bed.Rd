% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sparse_ld_bed_helper.R
\name{stblr_bed}
\alias{stblr_bed}
\title{Fit individual-level PLINK BED ST-BLR models}
\usage{
stblr_bed(
  y,
  Glist,
  method = c("bayesc", "bayesr"),
  covar = NULL,
  chr = NULL,
  cls = NULL,
  block_size = 1000,
  rows = NULL,
  scale = TRUE,
  nit = 1000,
  nburn = 100,
  nthin = 1,
  ncores = 1,
  seed = 1,
  nchains = 1L,
  chain_seeds = NULL,
  pi_init = 0.001,
  pi_vb_init = NULL,
  pi_prior_mean = 0.001,
  pi_prior_strength = 5e+05,
  pi_prior_a = NULL,
  pi_prior_b = NULL,
  h2 = 0.3,
  adjE = 0.9,
  updateB = TRUE,
  updateE = TRUE,
  updatePi = TRUE,
  mixture_var = c(0, 0.01, 0.1, 1),
  pi = NULL,
  alpha = NULL,
  nub = 4,
  nue = 4,
  rebuild_every = 25,
  full_sweep_every = 10,
  candidate_threshold = 0.001,
  candidate_lifetime = 20,
  skip_nulls_burnin_only = FALSE,
  null_skip_base = 50,
  null_skip_max = 200,
  return_wy = FALSE,
  return_r = FALSE,
  read_block_size = 64,
  progress_every = 0,
  ...
)
}
\arguments{
\item{y}{Phenotype vector or matrix. If `y` is a vector, `names(y)` must
contain individual IDs matching `Glist$ids`. If `y` is a matrix,
`rownames(y)` must contain matching IDs unless `nrow(y) == Glist$n`.}

\item{Glist}{A BED-backed qgg genotype list containing `bedfiles`, `n`,
`ids`, `rsids`, `rsidsLD`, and optionally `af`.}

\item{method}{Model family. Case-insensitive `"bayesc"` fits BayesC and is
the default; case-insensitive `"bayesr"` fits BayesR.}

\item{covar}{Optional covariates. Covariates are not currently adjusted by
this BED interface; pass pre-adjusted phenotypes.}

\item{chr}{Chromosome/file indices to fit. If `NULL`, all available
non-missing, non-empty entries in `Glist$bedfiles` are used.}

\item{cls}{Optional marker column indices.}

\item{block_size}{Marker block size used to construct update sets for a
single BED file.}

\item{rows}{Optional 1-based individual row indices into the BED file. When
omitted, subset phenotypes can instead be matched using `rownames(y)` and
`Glist$ids`.}

\item{scale}{Standardize BED markers using allele frequencies.}

\item{nit, nburn, nthin}{MCMC iteration controls.}

\item{ncores}{Number of OpenMP threads.}

\item{seed}{Sampler seed.}

\item{nchains}{Number of scheduled MCMC chains.}

\item{chain_seeds}{Optional explicit chain seeds. Not currently supported by
the BED scheduled-chain backends.}

\item{pi_init}{Initial BayesC marker inclusion probability.}

\item{pi_vb_init}{Inclusion probability used when initializing BayesC
marker-effect variance. Defaults to `pi_init`.}

\item{pi_prior_mean}{Prior mean for the BayesC marker inclusion probability.}

\item{pi_prior_strength}{Total Beta prior strength for the BayesC marker
inclusion probability.}

\item{pi_prior_a, pi_prior_b}{Optional explicit BayesC Beta prior shape
parameters. When supplied, these override `pi_prior_mean` and
`pi_prior_strength`.}

\item{h2}{Initial heritability.}

\item{adjE}{Residual adjustment factor.}

\item{updateB, updateE, updatePi}{Logical sampler update controls.}

\item{mixture_var}{BayesR mixture variance multipliers. The first value must
be zero and non-null values must be positive.}

\item{pi}{Initial BayesR mixture probabilities. Defaults to total non-null
probability 0.001 split equally across non-null components.}

\item{alpha}{BayesR Dirichlet prior shape vector for mixture probabilities.}

\item{nub, nue}{Prior degrees of freedom.}

\item{rebuild_every, full_sweep_every, candidate_threshold, candidate_lifetime}{Sampler scheduling and residual-rebuild controls.}

\item{skip_nulls_burnin_only}{Restrict null-marker skipping to burn-in.}

\item{null_skip_base, null_skip_max}{Null-marker scheduling controls.}

\item{return_wy, return_r}{Return optional sampler state.}

\item{read_block_size, progress_every}{Scheduled-chain BED reading and
progress controls.}

\item{...}{Unsupported arguments.}
}
\value{
A formatted ST-BLR fit with standard `dm`, `bm`, and `input` fields.
  BayesC fits include `log_cpo`, `mean_log_cpo`, `final_pi`, `mean_pi`, and
  chain-summary matrices. BayesR fits additionally include `comp_prob` and
  `dm_component_mean`.
}
\description{
Fits individual-level ST-BLR models directly from PLINK BED files referenced
by a BED-backed `Glist`. This is the individual-level analogue of
[stblr_csr()] for the plain BayesC and BayesR model families.
}
\details{
`method = "bayesc"` fits the BayesC BED scheduled-chain backend.
`method = "bayesr"` fits the BayesR BED scheduled-chain backend. For BayesR,
`dm` is `P(component > 0)`, `comp_prob` contains marker-by-component
posterior probabilities, and `dm_component_mean` contains posterior mean
component indices. BED scheduled-chain fits expose CPO/log-CPO diagnostics.
}
\examples{
\dontrun{
fitC_bed <- stblr_bed(
  y = y,
  Glist = Glist,
  method = "bayesC",
  nit = 1000,
  nburn = 100
)

fitR_bed <- stblr_bed(
  y = y,
  Glist = Glist,
  method = "bayesR",
  nit = 1000,
  nburn = 100
)
}
}
