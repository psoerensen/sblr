% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sparse_ld_bed_helper.R
\name{stblr_csr_bayesr}
\alias{stblr_csr_bayesr}
\title{Fit an Exact CSR Summary-Statistics BayesR Model}
\usage{
stblr_csr_bayesr(
  stats,
  Glist = NULL,
  ld_prefix = NULL,
  n = NULL,
  m = NULL,
  pi = NULL,
  mixture_var = c(0, 0.01, 0.1, 1),
  alpha = NULL,
  h2 = 0.3,
  adjE = 0.9,
  nit = 1000,
  nburn = 100,
  nthin = 1,
  ncores = 1,
  seed = 1,
  nchains = 1L,
  keep_chains = FALSE,
  chain_seeds = NULL,
  updateB = TRUE,
  updateE = TRUE,
  updatePi = TRUE,
  updateE_start = NULL,
  updateE_every = 1L,
  nub = 4,
  nue = 4,
  use_comp_init = FALSE,
  comp_init = NULL,
  use_r_init = FALSE,
  r_init = NULL,
  rebuild_r_before_updateE = FALSE,
  ...
)
}
\arguments{
\item{stats}{Summary-statistics object with `yy`, `ww`, `wy`, and usually
`n`/`m`, as used by [stblr_csr()].}

\item{Glist}{Optional genotype list containing `Glist$sparseLD$prefix`.
Required when `ld_prefix` is not supplied.}

\item{ld_prefix, n, m}{Optional low-level CSR LD prefix, sample sizes, and
marker count.}

\item{pi}{Initial BayesR mixture probabilities. Defaults to total non-null
probability 0.001 distributed evenly across non-null components.}

\item{mixture_var}{BayesR mixture variance multipliers. The first value must
be zero for the null component and remaining values must be positive.}

\item{alpha}{Dirichlet prior shapes for mixture probabilities. Defaults to
`pi * 5e5`.}

\item{h2}{Initial heritability.}

\item{adjE}{Residual adjustment factor.}

\item{nit, nburn, nthin}{MCMC iteration controls.}

\item{ncores}{Number of OpenMP threads.}

\item{seed}{Base sampler seed.}

\item{nchains}{Number of independent chains per trait.}

\item{keep_chains}{Logical; return compact per-chain BayesR summaries.}

\item{chain_seeds}{Optional integer seeds, one per chain.}

\item{updateB, updateE, updatePi}{Logical sampler update controls.}

\item{updateE_start}{Zero-based first iteration where `updateE` is allowed.
Defaults to 0.}

\item{updateE_every}{Positive update interval for `updateE`.}

\item{nub, nue}{Prior degrees of freedom.}

\item{use_comp_init, comp_init, use_r_init, r_init, rebuild_r_before_updateE}{Advanced initialization controls.}

\item{...}{Unsupported options. Passing scheduled CSR or LD-swap controls
through `...` errors clearly.}
}
\value{
A formatted ST-BLR BayesR fit.
}
\description{
Fits a supported exact CSR BayesR model for GWAS summary statistics and a
disk-backed sparse LD matrix. The standard posterior inclusion probability
`dm` is `P(component > 0)`, and `comp_prob` stores marker-by-component
posterior probabilities with `component_0` as the null component.
}
\details{
Exact CSR BayesR supports multiple chains, compact per-chain output with
`keep_chains = TRUE`, chain seeds, and `updateE = TRUE` with strict residual
SSE validation. Scheduled CSR BayesR and BayesR LD-swap/MH moves are not
currently supported by this interface.
}
