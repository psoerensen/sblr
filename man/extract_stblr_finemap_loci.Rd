% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extract-stblr-finemap-loci.R
\name{extract_stblr_finemap_loci}
\alias{extract_stblr_finemap_loci}
\title{Extract Fine-Mapping Loci from a Genome-Wide CSR ST-BLR Fit}
\usage{
extract_stblr_finemap_loci(
  fit,
  Glist,
  locus_sets,
  trait = 1,
  credible_sets = TRUE,
  coverage = 0.95,
  min_r2 = 0.5,
  pip_cutoff = 0.001,
  cs_mode = c("single", "multi"),
  min_signal_pip = 0.05,
  max_signals = Inf
)
}
\arguments{
\item{fit}{Genome-wide `stblr_csr()` fit containing marker PIPs in `fit$dm`
and posterior mean effects in `fit$bm`.}

\item{Glist}{Genotype/sparse LD object used to resolve marker chromosome and
position information. If `credible_sets = TRUE`, `Glist$sparseLD$prefix`
must identify a readable sparse LD CSR prefix.}

\item{locus_sets}{Named or unnamed list of marker vectors defining loci to
summarize, for example `cs_global$locus_sets` from
[make_stblr_credible_sets()].}

\item{trait}{Trait column name or index in `fit$dm` and `fit$bm`.}

\item{credible_sets}{Logical; construct credible sets from the genome-wide
PIPs restricted to each locus.}

\item{coverage, min_r2, pip_cutoff}{Credible-set construction parameters.}

\item{cs_mode}{Credible-set mode. `"single"` calls
[make_credible_sets_from_ld()]. `"multi"` calls
[make_multisignal_credible_sets_from_ld()].}

\item{min_signal_pip}{Minimum remaining lead-marker PIP required to start a
new signal when `cs_mode = "multi"`.}

\item{max_signals}{Maximum number of multi-signal credible sets per locus
when `cs_mode = "multi"`.}
}
\value{
A list of class `"stblr_finemap"` with `summary`, `markers`,
  optional `credible_sets`, `loci`, and `parameters`. Unlike
  [finemap_stblr_csr()], no `runs` element is returned because no local
  chains are run.
}
\description{
Summarizes predefined fine-mapping loci from an existing genome-wide
[stblr_csr()] posterior fit. This is a post-processing helper: it does not
run local MCMC, does not write temporary CSR subsets, and does not refit the
model. Marker-level PIPs and posterior mean effects are read directly from
`fit$dm` and `fit$bm`.
}
\details{
A typical workflow is to fit the genome-wide CSR model first, optionally with
LD-swap proposals, define loci from the fitted PIPs with
[make_stblr_credible_sets()], and then extract locus summaries:

```r
fitMH <- stblr_csr(
  stats = stats,
  Glist = Glist,
  pi_init = 0.001,
  pi_prior_a = 1,
  pi_prior_b = 1,
  h2 = 0.3,
  adjE = 0.9,
  nit = 1000,
  nburn = 100,
  ncores = 3,
  seed = 10,
  scheduled = FALSE,
  updateLDswap = TRUE,
  ld_swap_prob = 0.10,
  ld_swap_r2 = 0.05,
  ld_swap_moves = 5
)

cs_global <- make_stblr_credible_sets(
  fit = fitMH,
  Glist = Glist,
  trait = "D1",
  coverage = 0.95,
  min_r2 = 0.5,
  pip_cutoff = 0.001,
  locus_pip_cutoff = 0.01,
  max_locus_distance = 1e6
)

fm <- extract_stblr_finemap_loci(
  fit = fitMH,
  Glist = Glist,
  locus_sets = cs_global$locus_sets,
  trait = "D1",
  credible_sets = TRUE,
  cs_mode = "multi"
)
```
}
