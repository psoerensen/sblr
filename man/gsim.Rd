% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gsim.R
\name{gsim}
\alias{gsim}
\title{Simulate genomic data from an in-memory matrix or a qgg Glist}
\usage{
gsim(
  Glist = NULL,
  W = NULL,
  ids = NULL,
  rsids = NULL,
  A = NULL,
  architecture = c("bayesc", "bayesr", "major_polygenic", "maf_dependent", "clustered",
    "fixed"),
  nt = 1L,
  h2 = 0.5,
  vg = 1,
  pi = NULL,
  mixture_variances = NULL,
  marker_multipliers = NULL,
  n_causal = NULL,
  beta = NULL,
  alpha = NULL,
  annotation_model = c("none", "sbayesrc"),
  rg = NULL,
  re = 0,
  maf = NULL,
  maf_exponent = -0.5,
  block_id = NULL,
  n_hot_blocks = NULL,
  cluster_enrichment = 20,
  n = NULL,
  m = 1000L,
  maf_min = 0.05,
  maf_max = 0.5,
  n_annotations = 0L,
  annotation_types = NULL,
  annotation_prob = 0.1,
  seed = NULL,
  standardize_W = TRUE,
  scale_effects = TRUE,
  return_genotypes = FALSE,
  return_marker_probabilities = TRUE,
  compute_sumstats = FALSE,
  chunk_size = 5000L,
  getG_fun = NULL
)
}
\arguments{
\item{Glist}{Optional qgg genotype-list object.}

\item{W}{Optional in-memory genotype matrix. At most one of `Glist` and `W`
may be supplied. If both are NULL, independent binomial genotypes are
simulated.}

\item{ids}{Optional sample IDs. In Glist mode, `n` randomly selected IDs are
used when `ids` is NULL and `n` is smaller than the available sample size.}

\item{rsids}{Optional eligible marker IDs.}

\item{A}{Optional marker-by-annotation matrix. Row names, when present, are
matched to marker IDs. Binary and continuous columns may be mixed.}

\item{architecture}{One of `"bayesc"`, `"bayesr"`,
`"major_polygenic"`, `"maf_dependent"`, `"clustered"`, or `"fixed"`.}

\item{nt}{Number of traits.}

\item{h2}{Target trait heritabilities.}

\item{vg}{Target genetic variances. Marker effects are rescaled to these
realized variances when `scale_effects = TRUE`.}

\item{pi}{Mixture probabilities, with the null component first.}

\item{mixture_variances}{Relative mixture-component variances, with zero
first.}

\item{marker_multipliers}{Optional positive marker-specific relative
active-effect variance multipliers, \eqn{q_j}. A supplied vector must have
unique nonempty names exactly matching the simulation markers and is
aligned once to canonical marker order. The multiplier is applied only to
non-null effect draws, separately from component probabilities and the
component multiplier. `NULL` gives unit multipliers. Values are not
clipped or normalized. The aligned vector and compact provenance are
returned. SBayesRV is one possible research motivation for supplying this
generic fixed variance truth; `gsim()` does not implement SBayesRV
inference. Non-unit multipliers are not defined for
`architecture = "fixed"`, where `beta` supplies realized effects directly.}

\item{n_causal}{Optional exact number of non-null markers. If supplied,
markers are sampled without replacement using their annotation-informed
non-null probabilities.}

\item{beta}{Fixed marker effects for `architecture = "fixed"`.}

\item{alpha}{Annotation effects, with dimensions `ncol(A)` by
`length(pi)-1`. Effects act on centered annotations and continuation logits.}

\item{annotation_model}{`"none"` or `"sbayesrc"`.}

\item{rg, re}{Genetic-effect and residual correlation matrices (or scalar
off-diagonal correlations for multiple traits).}

\item{maf}{Optional marker MAFs, named by marker ID or in marker order.}

\item{maf_exponent}{For `maf_dependent`, effect variance is proportional to
`[2p(1-p)]^maf_exponent` before realized-variance calibration.}

\item{block_id}{Optional marker block labels for `clustered`.}

\item{n_hot_blocks}{Number of enriched blocks in the clustered architecture.}

\item{cluster_enrichment}{Active-odds multiplier in enriched blocks.}

\item{n, m}{Sample and marker counts when genotypes are simulated. In Glist
mode, `n = NULL` uses all available samples.}

\item{maf_min, maf_max}{MAF range for simulated genotypes.}

\item{n_annotations}{Number of annotations to simulate when `A` is NULL.}

\item{annotation_types}{Simulated annotation types: `"binary"` and/or
`"continuous"`.}

\item{annotation_prob}{Probability for simulated binary annotations.}

\item{seed}{Optional seed.}

\item{standardize_W}{Whether loaded causal genotypes are standardized.}

\item{scale_effects}{Whether effects are calibrated to `vg`.}

\item{return_genotypes}{Return causal genotype columns in `W_causal`.}

\item{return_marker_probabilities}{Return the full marker probability surface.}

\item{compute_sumstats}{Compute marginal GWAS statistics. Glist data are read
in chunks for this optional step.}

\item{chunk_size}{Maximum marker columns requested in each Glist read.}

\item{getG_fun}{Optional replacement for `qgg::getG`, primarily for tests.}
}
\value{
A list containing phenotypes, genetic values, residuals, exact marker
  effects and states, annotation truth, probability surfaces, the complete
  canonical `marker_multipliers` vector, compact multiplier provenance under
  `settings$marker_multipliers`, and optional summary statistics.
}
\description{
`gsim()` is the single public simulation entry point used by sblrbench. In
Glist mode, causal variants are selected from marker metadata before any
genotypes are read; only those causal columns are requested from
`qgg::getG()` to construct genetic values. Genome-wide genotypes are read in
chunks only when `compute_sumstats = TRUE`.
}
