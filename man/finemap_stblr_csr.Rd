% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/finemap-stblr-csr.R
\name{finemap_stblr_csr}
\alias{finemap_stblr_csr}
\title{Local Fine-Mapping for CSR ST-BLR Fits}
\usage{
finemap_stblr_csr(
  fit,
  Glist,
  stats,
  sets,
  trait = NULL,
  nruns = 8,
  use_residual = TRUE,
  nit = 20000,
  nburn = 2000,
  seeds = NULL,
  ve = NULL,
  vb = NULL,
  pi = NULL,
  credible_sets = TRUE,
  coverage = 0.95,
  min_r2 = 0.5,
  pip_cutoff = 0.001,
  cs_mode = c("single", "multi"),
  min_signal_pip = 0.05,
  max_signals = Inf,
  updateLDswap = FALSE,
  ld_swap_prob = 0.05,
  ld_swap_r2 = 0.8,
  ld_swap_max_friends = 50,
  ld_swap_moves = 1,
  verbose = FALSE
)
}
\arguments{
\item{fit}{Global `stblr_csr()` fit.}

\item{Glist}{Genotype/sparse LD object containing `Glist$sparseLD`.}

\item{stats}{Summary-statistics object used by `stblr_csr()`.}

\item{sets}{Named or unnamed list of marker vectors defining local regions.}

\item{trait}{Trait name or column index. If `NULL`, all traits in `fit$dm`
are fine-mapped.}

\item{nruns}{Number of independent local runs per region.}

\item{use_residual}{Logical; if `TRUE`, residualize local `wy` statistics
against global posterior mean effects outside the region.}

\item{nit, nburn}{Local MCMC iteration controls.}

\item{seeds}{Optional integer vector of length `nruns`. If `NULL`, seeds are
generated deterministically from `fit$input$seed` when available, otherwise
from `seq_len(nruns)`.}

\item{ve, vb, pi}{Optional scalar or named numeric global-parameter overrides.
`pi` is the marker inclusion probability, not `c(1 - pi, pi)`.}

\item{credible_sets}{Logical; construct credible sets from aggregated local
PIPs.}

\item{coverage, min_r2, pip_cutoff}{Credible-set construction parameters.}

\item{cs_mode}{Credible-set mode. `"single"` keeps the historical
single-signal behavior. `"multi"` uses approximate marginal-PIP
multi-signal post-processing via
[make_multisignal_credible_sets_from_ld()].}

\item{min_signal_pip}{Minimum remaining lead-marker PIP required to start a
new signal when `cs_mode = "multi"`.}

\item{max_signals}{Maximum number of multi-signal credible sets per
locus/trait when `cs_mode = "multi"`.}

\item{updateLDswap}{Logical; attempt optional LD-swap Metropolis-Hastings
moves among included markers and high-LD excluded neighbors. These moves
can improve mixing in fine-mapping and high-LD regions, are off by
default, and use an exact proposal-ratio correction in the base CSR
sampler.}

\item{ld_swap_prob}{Probability per MCMC iteration of attempting LD-swap
moves when `updateLDswap = TRUE`.}

\item{ld_swap_r2}{Minimum LD r-squared for candidate swap partners.}

\item{ld_swap_max_friends}{Maximum number of high-LD friends stored per
marker, prioritized by highest r-squared.}

\item{ld_swap_moves}{Number of swap attempts when LD-swap is triggered.}

\item{verbose}{Logical; print progress information for local fits.}
}
\value{
A list of class `"stblr_finemap"` with locus summaries,
  marker-level aggregated results, optional credible sets, locus metadata,
  compact run summaries, and resolved parameters.
}
\description{
Fine-map predefined local marker regions using the global architecture
learned from a previous [stblr_csr()] fit. This is a local fine-mapping
step, not a genome-wide refit: every marker supplied in each `sets` element
is included in that local model. `coverage`, `min_r2`, and `pip_cutoff` are
used only for optional credible-set construction after local fitting.
}
\details{
The local CSR sampler is run with fixed global parameters `ve`, `vb`, and
`pi` when available. By default these are resolved from the global fit
posterior traces after burn-in; user-supplied values override the fit. The
implementation calls the lower-level CSR sampler with `updateE = FALSE`,
`updateB = FALSE`, and `updatePi = FALSE`, so these parameters are fixed for
the local runs. `use_residual = TRUE` residualizes regional summary
statistics against posterior mean global effects outside the region.
}
