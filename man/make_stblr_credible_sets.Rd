% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/credible_sets.R
\name{make_stblr_credible_sets}
\alias{make_stblr_credible_sets}
\title{Construct Credible Sets for Fitted ST-BLR Models}
\usage{
make_stblr_credible_sets(
  fit,
  Glist = NULL,
  LD = NULL,
  sets = NULL,
  trait = 1,
  coverage = 0.95,
  min_r2 = 0.5,
  pip_cutoff = 0.001,
  locus_pip_cutoff = 0.01,
  max_locus_distance = 1e+06,
  max_loci = Inf,
  method = c("pip", "ld_pip")
)
}
\arguments{
\item{fit}{Fitted ST-BLR object containing marker PIPs in `fit$dm`.}

\item{Glist}{Optional genotype list containing marker map and `sparseLD`.}

\item{LD}{Optional dense LD matrix, or a named list of dense LD matrices for
multiple predefined sets.}

\item{sets}{Optional named or unnamed list of marker vectors defining
regions to analyze.}

\item{trait}{Trait column name or index when `fit$dm` is a matrix/data frame.}

\item{coverage, min_r2, pip_cutoff}{Credible-set construction parameters
passed to [make_credible_sets_from_ld()].}

\item{locus_pip_cutoff}{Minimum PIP for automatic lead-locus selection.}

\item{max_locus_distance}{Locus half-window in base pairs for automatic
locus definition.}

\item{max_loci}{Maximum number of automatic loci.}

\item{method}{Lead-marker selection method passed to
[make_credible_sets_from_ld()].}
}
\value{
A list with combined `summary`, nested credible `sets`, `loci`,
  `parameters`, and resolved `trait`.
}
\description{
Constructs credible sets from marker-level PIPs in `fit$dm` for fitted
ST-BLR models. This is an R-level post-processing function for fits from
`stblr_csr()` and `stblr_bed_marker()`.
}
\details{
Credible sets can be built from predefined marker sets or from automatically
defined loci around high-PIP markers. Dense LD can be supplied directly for
regional credible sets. Alternatively, for CSR workflows, `Glist$sparseLD`
can be used and regional dense LD matrices are extracted internally from the
disk-backed sparse CSR.

When using sparse LD from `Glist$sparseLD`, the sparse LD should have been
computed with a sufficiently permissive `r2_threshold` and local window to
support the requested `min_r2`; otherwise, LD pairs needed for set
construction may be absent.
}
