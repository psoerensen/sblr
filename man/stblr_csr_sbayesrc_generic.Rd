% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stblr-csr-sbayesrc.R
\name{stblr_csr_sbayesrc_generic}
\alias{stblr_csr_sbayesrc_generic}
\title{Fit an SBayesRC-Style Annotation-Dependent BayesR Prior}
\usage{
stblr_csr_sbayesrc_generic(
  stats,
  ld_prefix,
  A,
  n = NULL,
  m = NULL,
  gamma = c(0, 0.01, 0.1, 1),
  pi_marker = 0.001,
  pi_init = NULL,
  pi_vb_init = NULL,
  pi_prior_mean = NULL,
  pi_prior_strength = NULL,
  pi_prior_a = NULL,
  pi_prior_b = NULL,
  h2 = 0.5,
  nub = 4,
  nue = 4,
  B = NULL,
  E = NULL,
  ssb_prior = NULL,
  sse_prior = NULL,
  updateAlpha = TRUE,
  updateB = TRUE,
  updateE = TRUE,
  adjE = 0.9,
  nit = 1000,
  nburn = 100,
  nthin = 1,
  ncores = 3,
  seed = 10,
  nchains = 1L,
  chain_seeds = NULL,
  keep_chains = FALSE,
  updateLDswap = FALSE,
  ld_swap_prob = 0.05,
  ld_swap_r2 = 0.8,
  ld_swap_max_friends = 50L,
  ld_swap_moves = 1L,
  b_init = NULL,
  comp_init = NULL,
  use_comp_init = FALSE,
  r_init = NULL,
  use_r_init = FALSE,
  rebuild_r_before_updateE = FALSE,
  add_intercept = TRUE,
  standardize_annotations = TRUE,
  center_binary_annotations = FALSE,
  active_comp_weights = NULL,
  alpha_init = NULL,
  sigmaSqAlpha_init = NULL,
  intercept_flat = TRUE,
  sigmaSqAlpha_a = 2,
  sigmaSqAlpha_b = 2,
  pi_floor = 1e-12,
  alpha_update_every = 10,
  selection_s = NULL,
  estimate_selection_s = FALSE,
  selection_s_init = 0,
  selection_s_prior = c(-3, 2),
  selection_s_proposal_sd = 0.35,
  Glist = NULL
)
}
\arguments{
\item{stats}{Sufficient statistics returned by [bed_xtx_xty()].}

\item{ld_prefix}{Prefix of the disk-backed CSR LD files.}

\item{A}{An `m` by `K` numeric marker annotation matrix. Rows must correspond
to the markers in `stats` and the sparse LD files.}

\item{n}{Sample size. Defaults to `stats$n` when available.}

\item{m}{Number of markers. Inferred from `stats` when omitted.}

\item{gamma}{Numeric mixture variance multipliers. The first value must be
zero and all remaining values must be positive.}

\item{pi_marker}{Backward-compatible initial active-marker probability.}

\item{pi_init, pi_vb_init, pi_prior_mean, pi_prior_strength, pi_prior_a, pi_prior_b}{Active-marker probability and marker-variance prior controls.}

\item{h2}{Initial heritability.}

\item{nub, nue}{Prior degrees of freedom.}

\item{B, E}{Optional initial marker-effect and residual covariance matrices.}

\item{ssb_prior, sse_prior}{Optional prior scale matrices.}

\item{updateAlpha, updateB, updateE}{Logical sampler update controls.}

\item{adjE}{Residual adjustment factor.}

\item{nit, nburn, nthin}{MCMC iteration controls.}

\item{ncores}{Number of OpenMP threads.}

\item{seed}{Sampler seed.}

\item{nchains}{Number of independent MCMC chains.}

\item{chain_seeds}{Optional integer seeds, one per chain.}

\item{keep_chains}{Logical; return compact per-chain summaries.}

\item{updateLDswap}{Logical; attempt optional active/null LD-swap
Metropolis-Hastings moves.}

\item{ld_swap_prob}{Probability per MCMC iteration of attempting LD-swap
moves when `updateLDswap = TRUE`.}

\item{ld_swap_r2}{Minimum LD r-squared for candidate swap partners.}

\item{ld_swap_max_friends}{Maximum number of high-LD friends stored per
marker for swap proposals.}

\item{ld_swap_moves}{Number of swap attempts when LD-swap is triggered.}

\item{b_init}{Optional initial marker effects.}

\item{comp_init}{Optional initial zero-based mixture-component indices, one
length-`m` vector per trait.}

\item{use_comp_init}{Use the supplied initial component indices.}

\item{r_init}{Optional initial residual state.}

\item{use_r_init}{Use the supplied initial residual state.}

\item{rebuild_r_before_updateE}{Rebuild the residual before updating `E`.}

\item{add_intercept}{Add an intercept column to `A` when one is not present.}

\item{standardize_annotations}{Standardize non-binary annotation columns.}

\item{center_binary_annotations}{Center and standardize binary annotations.}

\item{active_comp_weights}{Optional weights distributing `pi_init` across
the active mixture components.}

\item{alpha_init}{Optional initial annotation coefficient matrix.}

\item{sigmaSqAlpha_init}{Optional initial annotation-effect variances.}

\item{intercept_flat}{Use a flat prior for the intercept coefficients.}

\item{sigmaSqAlpha_a, sigmaSqAlpha_b}{Annotation-effect variance prior
controls.}

\item{pi_floor}{Lower probability bound used by the sampler.}

\item{alpha_update_every}{Iterations between annotation-effect updates.}

\item{selection_s}{Optional fixed global BayesS-style MAF-scaling parameter.
The default `selection_s = NULL` with `estimate_selection_s = FALSE` fits
the ordinary SBayesRC model. A finite numeric scalar with
`estimate_selection_s = FALSE` fits a fixed global-S model. `selection_s`
must remain `NULL` when `estimate_selection_s = TRUE`; fixed and sampled S
cannot both be requested.}

\item{estimate_selection_s}{Logical; estimate one trait-specific
BayesS-style `selection_s` by Metropolis-Hastings for CSR SBayesRC. Fixed
`selection_s` and sampled `selection_s` are mutually exclusive. Sampled S
is supported for CSR SBayesRC, but not for the BayesC-like annotation-aware
CSR backends.}

\item{selection_s_init}{Initial value for sampled `selection_s`. Defaults to
0 and is used only when `estimate_selection_s = TRUE`.}

\item{selection_s_prior}{Numeric length-2 lower and upper bounds for the
uniform sampled-`selection_s` prior. Only used when
`estimate_selection_s = TRUE`. Defaults to `c(-3, 2)`.}

\item{selection_s_proposal_sd}{Random-walk proposal standard deviation for
sampled `selection_s`. Only used when `estimate_selection_s = TRUE`.
Defaults to 0.35.}

\item{Glist}{Optional genotype/LD metadata. Required when `selection_s` is
non-`NULL` or `estimate_selection_s = TRUE` so MAF can be aligned to the
CSR LD marker order using `Glist$rsidsLD`, `Glist$rsids`, and `Glist$maf`.}
}
\value{
A formatted SBayesRC-style ST-BLR fit with annotation and
  mixture-component posterior summaries, including `comp_prob`,
  `dm_component_mean`, `alpha`, `sigmaSqAlpha`, and `ncomp` where available.
  The fit includes `vle` and `vld` traces using the same definitions and
  formatting conventions as annotation-unaware CSR fits.
  For sampled `selection_s`, the fit also contains `selection_s`,
  `selection_s_sd`, `selection_s_min`, `selection_s_max`,
  `selection_s_trace`, and `selection_s_acceptance`. `selection_s_trace` is
  an iteration x trait matrix, `selection_s` is the posterior mean by trait,
  and `selection_s_acceptance` is the MH acceptance rate by trait. With
  `keep_chains = TRUE`, chain-level sampled-S output is available as
  `fit$chains[[trait]][[chain]]$selection_s` and
  `fit$chains[[trait]][[chain]]$selection_s_acceptance`.
}
\description{
Fits a CSR ST-BLR model with an SBayesRC-style annotation-dependent BayesR
prior. Marker mixture probabilities are modeled through probit
stick-breaking regressions on an annotation matrix. The core prior family is
closely aligned with the published SBayesRC prior structure, but the exposed
default gamma grid and CSR sparse-LD likelihood differ from the published
GCTB SBayesRC implementation.
}
\details{
CSR effects are on the standardized-genotype scale. The BayesS-style
MAF-dependent prior scale used by fixed and sampled `selection_s` is
`q_j(S) = h_j^(S + 1)`, where `h_j = 2 p_j (1 - p_j)` and `p_j` is the
minor allele frequency. The `+1` exponent appears because the sampler effects
are standardized-genotype-scale effects rather than allele-scale effects.

For fixed `selection_s`, CSR SBayesRC uses
`b_j | component_j = m, vb, S ~ N(0, vb * gamma_m * q_j(S))`.
Annotations affect component probabilities, while `selection_s` affects
marker-specific effect-size prior variance. The null component column is
`gamma_0.00` and `dm = 1 - P(gamma_0.00)`.

Sampled CSR SBayesRC uses the trait-specific MH log posterior contribution

```text
log p(S_t | b_t, gamma_t, vb_t)
  = log p(S_t)
    - 0.5 sum_\{j: gamma_jt > 0\} [
        log q_j(S_t) + b_jt^2 / (vb_t gamma_jt q_j(S_t))
      ]
```

`S_t` is sampled separately for each trait and chain. Posterior summaries
are averaged or summarized across chains in the returned fit object.
}
\examples{
\dontrun{
fit_sampled_s_sbayesrc <- stblr_csr_annot(
  stats = stats,
  Glist = Glist,
  annotations = annotations,
  annotation_model = "sbayesrc",
  estimate_selection_s = TRUE,
  selection_s_prior = c(-3, 2),
  selection_s_proposal_sd = 0.35
)
}
}
