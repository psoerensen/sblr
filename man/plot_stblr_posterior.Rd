% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sparse_ld_bed_helper.R
\name{plot_stblr_posterior}
\alias{plot_stblr_posterior}
\title{Plot ST-BLR Posterior Summaries}
\usage{
plot_stblr_posterior(
  post,
  parameters = NULL,
  traits = NULL,
  interval = c("hpd", "quantile", "both"),
  point = c("mean", "median"),
  facet_by = c("parameter", "trait"),
  log_scale = FALSE,
  xlab = NULL,
  main = NULL,
  cex_axis = 0.8,
  pch = 19,
  lwd_hpd = 4,
  lwd_quantile = 1,
  col_point = "black",
  col_hpd = "gray35",
  col_quantile = "gray70",
  parse_labels = TRUE
)
}
\arguments{
\item{post}{Posterior summary data frame from
[summarise_stblr_posterior()].}

\item{parameters}{Optional character vector of internal parameter names to
plot.}

\item{traits}{Optional character vector of traits to plot.}

\item{interval}{Interval type to plot: `"hpd"`, `"quantile"`, or `"both"`.}

\item{point}{Point estimate to plot: `"mean"` or `"median"`.}

\item{facet_by}{Faceting variable: `"parameter"` or `"trait"`.}

\item{log_scale}{Logical; use a log-scaled x-axis when possible.}

\item{xlab}{Optional x-axis label.}

\item{main}{Optional panel title override.}

\item{cex_axis}{Axis-label character expansion.}

\item{pch}{Point plotting character.}

\item{lwd_hpd, lwd_quantile}{Line widths for HPD and equal-tail intervals.}

\item{col_point, col_hpd, col_quantile}{Colors for points, HPD intervals, and
equal-tail intervals.}

\item{parse_labels}{Logical; parse parameter labels as plotmath
expressions.}
}
\value{
Invisibly returns the filtered data frame used for plotting.
}
\description{
Plots posterior means or medians with HPD intervals, equal-tail intervals,
or both interval types from the data frame returned by
[summarise_stblr_posterior()]. The `parameters` argument uses internal
parameter names such as `vg`, `ve`, `h2`, `pi`, `vb`, and `varch`, while plot
labels use the `label` column or built-in plotmath labels.
}
\details{
The function uses base R graphics.
}
\examples{
fake_post <- data.frame(
  parameter = c("vg", "ve", "h2", "pi", "varch"),
  label = c("V[g]", "V[e]", "h^2", "pi", "V[arch]"),
  trait = "trait1",
  n = 20L,
  mean = c(0.6, 0.4, 0.6, 0.02, 0.1),
  median = c(0.6, 0.4, 0.6, 0.02, 0.1),
  sd = 0.01,
  mcse = 0.002,
  q_lower = c(0.55, 0.35, 0.55, 0.01, 0.08),
  q_upper = c(0.65, 0.45, 0.65, 0.03, 0.12),
  hpd_lower = c(0.56, 0.36, 0.56, 0.01, 0.08),
  hpd_upper = c(0.64, 0.44, 0.64, 0.03, 0.12),
  ess = NA_real_,
  autocorr_lag1 = NA_real_,
  min = c(0.54, 0.34, 0.54, 0.01, 0.07),
  max = c(0.66, 0.46, 0.66, 0.03, 0.13)
)
if (interactive()) {
  plot_stblr_posterior(fake_post, parameters = c("vg", "ve", "h2"))
}

}
