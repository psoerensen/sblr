% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sparse_ld_bed_workflow.R
\name{stblr_bed_marker}
\alias{stblr_bed_marker}
\title{Fit ST-BLR Directly from PLINK BED Markers}
\usage{
stblr_bed_marker(
  Glist,
  y,
  chr = 1:22,
  cls = NULL,
  block_size = 1000,
  pi_init = 0.001,
  pi_vb_init = NULL,
  pi_prior_mean = 0.01,
  pi_prior_strength = 5e+05,
  pi_prior_a = NULL,
  pi_prior_b = NULL,
  h2 = 0.5,
  nub = 4,
  nue = 4,
  scale = TRUE,
  updateB = TRUE,
  updateE = TRUE,
  updatePi = TRUE,
  adjE = 0,
  nit = 1000,
  nburn = 100,
  nthin = 1,
  rebuild_every = 25,
  full_sweep_every = 10,
  candidate_threshold = 0.001,
  candidate_lifetime = 20,
  skip_nulls_burnin_only = FALSE,
  ncores = 1,
  seed = 10,
  backend = c("auto", "scheduled", "sparse"),
  null_update_prob = 0.02,
  null_skip_base = 50,
  null_skip_max = 200,
  return_wy = FALSE,
  return_r = FALSE,
  rows = NULL,
  nchains = 1,
  read_block_size = 64,
  progress_every = 0
)
}
\arguments{
\item{Glist}{A qgg genotype list.}

\item{y}{Phenotype matrix.}

\item{chr}{Chromosome indices to fit.}

\item{cls}{Optional marker column indices.}

\item{block_size}{Marker block size.}

\item{pi_init}{Initial marker inclusion probability.}

\item{pi_vb_init}{Inclusion probability used when initializing marker-effect
variance. Defaults to `pi_init`.}

\item{pi_prior_mean}{Prior mean for the marker inclusion probability.
Defaults to 0.01.}

\item{pi_prior_strength}{Total Beta prior strength for the marker inclusion
probability. Defaults to 5e5.}

\item{h2}{Initial heritability.}

\item{nub, nue}{Prior degrees of freedom.}

\item{scale}{Standardize BED markers.}

\item{updateB, updateE, updatePi}{Logical sampler update controls.}

\item{adjE}{Residual adjustment factor.}

\item{nit, nburn, nthin}{MCMC iteration controls.}

\item{rebuild_every, full_sweep_every, candidate_threshold, candidate_lifetime}{Sparse sampler controls.}

\item{skip_nulls_burnin_only}{Restrict null-marker skipping to burn-in.}

\item{ncores}{Number of OpenMP threads.}

\item{seed}{Sampler seed.}

\item{backend}{Sampler backend. `"auto"` selects the scheduled multi-chain
backend for multi-chromosome fits or when `nchains > 1`, and the scheduled
single-chain backend otherwise. `"scheduled"` forces a scheduled backend;
for multi-chromosome fits it uses the scheduled multi-chain backend.
`"sparse"` is only valid for one chromosome.}

\item{null_update_prob, null_skip_base, null_skip_max}{Null-marker scheduling
controls.}

\item{return_wy, return_r}{Return optional sampler state.}

\item{rows}{Optional 1-based individual row indices into the BED file. When
omitted, subset phenotypes can instead be matched using `rownames(y)` and
`Glist$ids`.}

\item{nchains}{Number of MCMC chains. When `nchains > 1`, the scheduled
multi-chain backend is used.}

\item{read_block_size, progress_every}{Multi-chain BED reading and progress
controls.}
}
\value{
A formatted ST-BLR fit.
}
\description{
Fits ST-BLR directly from markers stored in PLINK BED files referenced by a
qgg genotype list.
}
